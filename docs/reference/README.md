# Reference screenshots

Captured from the Burning Crusade Anniversary client. They define the Classic
look for each element, so "pixel-faithful" has a concrete target.

Capture at UI scale 1.0 and 100% render scale. Crop tightly to the element.
Name the file after the element, kebab-case.

| Element | File | Status |
| --- | --- | --- |
| Player frame | `player-frame.png` | needed |
| Target frame | `target-frame.png` | needed |

Capture the player frame at full health and with a debuff active. Capture the
target frame on a hostile elite, so the level ring and classification art show.
